export default function ChatPreview() {
    // preview of a chat including avatar, name and first few words in chat
    // there should be a list of ChatPreviews upon logging in
    // otherwise conditionally render "No Chats"
}